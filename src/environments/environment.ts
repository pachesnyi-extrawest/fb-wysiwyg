// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCvNPdEMOG-H-mwz13qARgbeD-IpHMAhLQ",
    authDomain: "fb-wysiwyg.firebaseapp.com",
    databaseURL: "https://fb-wysiwyg.firebaseio.com",
    projectId: "fb-wysiwyg",
    storageBucket: "fb-wysiwyg.appspot.com",
    messagingSenderId: "314902449023",
    appId: "1:314902449023:web:53fc48e609a631d8ca684a"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
